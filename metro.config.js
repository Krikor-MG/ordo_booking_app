// metro.config.js
const { getDefaultConfig } = require('expo/metro-config');

const config = getDefaultConfig(__dirname);

// If you need SVG support or extra asset extensions, you can tweak here.

module.exports = config;
